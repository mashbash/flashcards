<h1>Please choose your topic</h1>
<form name="decklist" action="/rounds" method="post">
  <% @decks.each do |deck| %>
    <input class="radio" type="radio" name="id" value="<%= deck.id %>"> <%= deck.topic %><br>
  <% end %>
  <div class='button'><input class="button inline-button" type='submit' value='Start game!'/></div>
</form>